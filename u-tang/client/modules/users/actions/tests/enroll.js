const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../enroll';

describe('users.actions.enroll', () => {
  it('should do something');
});
